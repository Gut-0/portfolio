// Tweakable props from the design handoff (README → "Tweakable props").
// accent is themed via the --accent CSS variable in index.css.

export const DEFAULT_PHASE: 1 | 2 = 1;
export const REVEAL_ANIMATIONS = true;
export const CURSOR_GLOW = true;

export const REPO_URL = "https://github.com/Gut-0/portfolio";
